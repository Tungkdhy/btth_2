import { SplitUnitNameFullPipe } from './split-unit-name-full.pipe';

describe('SplitUnitNameFullPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitUnitNameFullPipe();
    expect(pipe).toBeTruthy();
  });
});
